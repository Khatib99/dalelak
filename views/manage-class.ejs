<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>DALELAK</title>
	<link rel="stylesheet" href="css/manage-class.css">
	<script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
</head>
<body>
	<div class="back">
		<a href="/admin"><button class="back-button"><i id="icon" class="material-icons">arrow_back</i></button></a>

	</div>
	<% if (status){ %>
		<script>window.alert('Can\'t delete a class with a student enroll in it .!!')</script>
	<% } %>
	<div class="main-container">
		<div class="nav">
			<h3>manage-class</h3>
		</div>
		<div class="class-Section">
			<% for (var i =0; i < classes.length;  i++ ) { %>
				<form action="delete-class" method='post'>
					<input type="text" class='hidden-input' value=<%= classes[i].grade %> name='grade'>
					<input type="text" class='hidden-input' value=<%= classes[i].section %> name='section'>
					<div class="section">
						<div><%= classes[i].grade %> nth</div>
						<div><%= classes[i].section %></div>
						<button type="submit" class="add-button"><i id="icon" class="material-icons">delete</i></button>
					</div>
				</form>
			<% } %>
		</div>
		<div class="add">
			<a href="/add-class"><button class="add-button"><i id="icon" class="material-icons">add</i></button></a>
		</div>
	</div>
</body>
</html>